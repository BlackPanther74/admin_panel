<h1>Add Student</h1>
<%= link_to "Back", :back, class: "btn btn-secondary" %>
<hr>

<div class="row">
  <div class="col-sm-4">
  </div>


  <div class="col-sm-4">
    <div class="card">
      <div class="card-body">
        <%= bootstrap_form_with(model: @student) do |form| %>
          <%= form.text_field :first_name, label: "First Name:" %>
          <%= form.text_field :last_name, label: "Last Name:" %> 
          <%= form.text_field :age, label: "Age:" %>
          <%= form.select :education, ['High School', 'College', 'Masters', 'PhD'], include_blank: true, label: "Education:" %>
          <%= form.submit class: "btn btn-primary", value: "Add Student" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
<hr>
